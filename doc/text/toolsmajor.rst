These are various programs that I have found useful, but can't commit to fully
maintaining.
These take a single argument on the command line, which names a JSON file. For example,

.. highlight:: none

::

    addNoise noiseConf.json

