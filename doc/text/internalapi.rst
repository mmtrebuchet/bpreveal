These are modules that are used by BPReveal but that you probably don't need to mess with.
Nevertheless, you can always import them yourself::

    from bpreveal.internal import disableTensorflowLogging

